### web_search
Search the web via Exa. Returns titles, URLs, and highlight snippets.

Use when: you need to discover URLs or scan a topic.
Not when: you already have a URL — use `web_fetch` instead.

Examples:
- {"query": "latest news about X", "n": 5, "type": "auto"}
